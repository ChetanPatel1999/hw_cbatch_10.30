// strlen() :- its return length of string
#include <stdio.h>
#include <string.h>
void main()
{
    char color[] = "red";
    printf("color = %s\n", color);
    printf("color length = %d\n", strlen(color));
}